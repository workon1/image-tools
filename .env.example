# Public site URL used for canonical links, Open Graph, robots.txt, and sitemap.xml.
# Local development:
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Production builds use https://imagereshaper.com (see .env.production).
# On Vercel, set NEXT_PUBLIC_SITE_URL=https://imagereshaper.com for Production.

# Public mailbox shown on the Contact page. Forward this address with Cloudflare Email Routing.
NEXT_PUBLIC_CONTACT_EMAIL=hello@imagereshaper.com

# Analytics. Off in local .env.example; production uses GA4 (see .env.production).
# The app never sends image contents, filenames, or personal data.
NEXT_PUBLIC_ANALYTICS_ENABLED=false
# none | console | ga4
NEXT_PUBLIC_ANALYTICS_PROVIDER=none
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Ads stay off until AdSense approves the site. Then set client id, inline slot id, and enable ads.
NEXT_PUBLIC_ADS_ENABLED=false

# Public AdSense publisher id (ca-pub-...) and one display ad unit id for inline tool slots.
NEXT_PUBLIC_ADSENSE_CLIENT_ID=
NEXT_PUBLIC_ADSENSE_INLINE_SLOT=

# Reserved for future premium / API features. Unused in the MVP.
NEXT_PUBLIC_PREMIUM_ENABLED=false
NEXT_PUBLIC_API_ENABLED=false
NEXT_PUBLIC_AFFILIATE_ENABLED=false
